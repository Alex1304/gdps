{% extends 'base.html.twig' %}

{% block content %}
	{% for user in users %}
		1:{{ user.username }}:
		2:{{ user.player.id }}:
		9:{{ user.player.icon }}:
		10:{{ user.player.color1 }}:
		11:{{ user.player.color2 }}:
		14:{{ user.player.iconType }}:
		15:{{ user.player.special }}:
		16:{{ user.id }}:
		18:0:
		41:{% if not isFriendList or user in newFriends %}1{% else %}0{% endif %}:
		{% if not loop.last %}|{% endif %}
	{% endfor %}
{% endblock %}