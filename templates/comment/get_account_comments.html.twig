{% extends 'base.html.twig' %}

{% block content %}
	{% for comment in comments %}
		2~{{ comment.content }}~
		4~{{ comment.likes }}~
		9~{{ timeFormatter.format(comment.postedAt) }}~
		6~{{ comment.id }}
		{% if not loop.last %}|{% endif %}
	{% endfor %}
	#
	{{ total }}:{{ page * count }}:{{ count }}
{% endblock %}