{% extends 'base.html.twig' %}

{% block content %}
	1:{{ account.username }}:
	2:{{ account.player.id }}:
	13:{{ account.player.coins }}:
	17:{{ account.player.userCoins }}:
	10:{{ account.player.color1 }}:
	11:{{ account.player.color2 }}:
	3:{{ account.player.stars }}:
	46:{{ account.player.diamonds }}:
	4:{{ account.player.demons }}:
	8:{{ account.player.creatorPoints }}:
	18:{{ account.privateMessagePolicy }}:
	19:{{ account.friendRequestPolicy }}:
	50:{{ account.commentHistoryPolicy }}:
	20:{{ account.youtube }}:
	21:{{ account.player.accIcon }}:
	22:{{ account.player.accShip }}:
	23:{{ account.player.accBall}}:
	24:{{ account.player.accUFO }}:
	25:{{ account.player.accWave }}:
	26:{{ account.player.accRobot }}:
	28:{{ account.player.accGlow }}:
	43:{{ account.player.accSpider }}:
	47:{{ account.player.accExplosion }}:
	30:{{ globalRank }}:
	16:{{ account.id }}:
	31:{{ friendState }}:
	44:{{ account.twitter }}:
	45:{{ account.twitch }}:
	{% if self %}
		38:{{ notifCounters.messages }}:
		39:{{ notifCounters.friendreqs }}:
		40:{{ notifCounters.friends }}:
	{% endif %}
	29:1:
	49:0
	{% if incomingFR is not null %}
		:
		32:{{ incomingFR.id }}:
		35:{{ incomingFR.message }}:
		37:{{ timeFormatter.format(incomingFR.madeAt) }}
	{% endif %}
{% endblock %}